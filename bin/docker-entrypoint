#!/bin/bash -e

# デバッグ出力を有効化
set -x

echo "Starting entrypoint script..."

# データベース接続テスト（非ブロッキングで）
bundle exec rails db:version || echo "Database version check failed, but continuing..."

# サーバー起動
echo "Starting Rails server on port 8080..."
exec "${@}"